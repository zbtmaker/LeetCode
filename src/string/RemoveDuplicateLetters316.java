package string;

/**
 * @author Baitao Zou
 * date 2021/04/27
 */
public class RemoveDuplicateLetters316 {
    /**
     * 是否可以认为是既要包括所有的字符串，同时，也需要判断这个自字符串之后能不能包含所有的字符串
     * 那么就可以用暴力的方式来解决，第一步，我们统计字符串中需要保存的字符串不同的数量
     * 如果在某个位置能够找到一个比其小的字符串
     *
     * @param s
     * @return
     */
    public String removeDuplicateLetters(String s) {

        return "";
    }
}
